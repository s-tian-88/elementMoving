import GamePlay from '../class/GamePlay';

describe('testing GamePlay module', () => {
    test('testing init GamePlay instance', () => {

    }),
    test('testing InnerHTML method from GamePlay class', () => {

        expect();
    }),
})

